# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=true

# Fabric Properties
# check these on https://fabricmc.net/develop
# The Minecraft version and the matching Fabric API live in versions/<mc>/build.gradle, one pair
# per supported game version, so that `gradlew build` produces a jar for each of them.
loader_version=0.19.5
loom_version=1.18-SNAPSHOT

# Mod Properties
version=1.0.0
group=com.dropcount
